第0章 学习设计模式的准备工作
本部分内容为自己补充.内容来源为其他书籍,参考书目如下:
    《敏捷软件开发 原则、模式与实践》
    《图解设计模式》
    《深入PHP面向对象、模式与实践》
本部分内容共分2章:
    Chapter1. UML作图
    Chapter2. 设计模式的五大原则

0.1 UML作图
    类、属性、操作、继承与实现部分的图例与说明:UML各部分样例-类、属性、操作、继承与实现
    关联、聚合与组合、使用部分的图例与说明:UML各部分样例-关联、聚合与组合、使用
    注解部分的图例与说明:UML各部分样例-注解

0.2 设计模式五大原则
    0.2.1 单一职责原则(SRP)
        Single Responsibility Principle:就一个类而言,应该仅有一个能引起它变化的原因.

    0.2.2 开放-封闭原则(OCP)
        Open-Closed Principle:软件实体(类、模块、函数等等)应该是可以扩展的,但是不可修改的.

    0.2.3 Liskov(里氏)替换原则(LSP)
        Liskov Substitution Principle:子类型(subtype)必须能够替换掉他们的基类型(base type).
        这里需要如下替换性质:若对每个类型S的对象o1,都存在一个类型T的对象o2,使得在所有针对T编写的程序P中,
        用o1替换o2后,程序P行为功能不变,则S是T的子类型.

    0.2.4 依赖倒置原则(DIP)
        Dependence Inversion Principle:
            a. 高层模块不应该依赖于低层模块.二者都应该依赖于抽象.
            b. 抽象不应该依赖于细节.细节应该依赖于抽象.

    0.2.5 接口隔离原则(ISP)
        Interface Segregation Principle:不应该强迫客户依赖于它们不用的方法.